{
  "name": "@tallerpro/backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "convex dev",
    "setup": "convex dev --once && convex dev --run init",
    "deploy": "convex deploy",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "convex": "^1.25.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.3"
  }
}
